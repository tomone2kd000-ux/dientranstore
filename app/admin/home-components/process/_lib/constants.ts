import type { ProcessConfig } from '../_types';

export const DEFAULT_PROCESS_CONFIG: ProcessConfig = {
  steps: [
    {
      description: '',
      icon: 'Star',
      title: '',
    },
  ],
  style: 'horizontal',
};
