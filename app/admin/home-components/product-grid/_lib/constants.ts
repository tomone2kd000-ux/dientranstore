'use client';

import type { ProductGridConfig } from '../_types';

export const DEFAULT_PRODUCT_GRID_CONFIG: ProductGridConfig = {
  itemCount: 8,
  sectionTitle: 'Sản phẩm nổi bật',
  selectedProductIds: [],
  selectionMode: 'auto',
  sortBy: 'newest',
  style: 'commerce',
  subTitle: 'Bộ sưu tập',
};
